// nextflow.config for nf-core/proteinannotator
// Branch: olgabot/interproscan-batches (PR #79)

// Docker with Apple Silicon compatibility
docker {
    enabled = true
    runOptions = '--platform linux/amd64'
}

process {
    errorStrategy = 'retry'
    maxRetries = 2

    withName: '.*:INTERPROSCAN' {
        memory = '64.GB'
        cpus = 16
        time = '24.h'
        // Xmx must be well below container memory limit to leave room for JVM overhead,
        // native processes, and InterProScan subprocesses — exit 231 = Java OOM
        containerOptions = '--env JAVA_OPTS="-Xmx56G"'
        errorStrategy = 'retry'
        maxRetries = 2
    }
}

report {
    enabled = true
    overwrite = true
}

timeline {
    enabled = true
    overwrite = true
}

trace {
    enabled = true
    overwrite = true
}

dag {
    enabled = true
    overwrite = true
}
